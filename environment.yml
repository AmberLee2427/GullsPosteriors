name: GullsPosteriors
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - numpy
  - astropy
  - matplotlib
  - scipy
  - pandas
  - astroquery
  - dynesty
  - corner
  - emcee
  - pip
  - pip:
      - VBMicrolensing
prefix: /opt/anaconda3/envs/GullsPosteriors
